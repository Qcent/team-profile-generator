const generateTeamHTML = (team) => {
    console.log(team);

    //output html boilerplate and site headder

    //foreach team member generate a card for their info


}

module.exports = generateTeamHTML;